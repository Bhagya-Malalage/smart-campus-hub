spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.hxyocwbfylhajfdvmmdv
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Change port to avoid Vite proxy conflicts
server.port=8082

# Allow the app to start even if the database is unreachable initially
spring.datasource.hikari.initialization-fail-timeout=-1

